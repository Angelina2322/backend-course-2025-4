{
  "name": "backend-course-2025-4",
  "version": "1.0.0",
  "description": "Проєкт для роботи з командним рядком і веб-сервером.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon index.js -- --host 127.0.0.1 --port 3000 --input ./weather.json"
  },
  "keywords": [
    "node",
    "commander",
    "http"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^14.0.1",
    "fast-xml-parser": "^4.4.1" 
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
